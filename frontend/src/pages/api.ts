// In a separate file (e.g., api.ts)
// import axios from 'axios';

// const api = axios.create({
 // baseURL: '/api',
 // headers: {
   // 'Content-Type': 'application/json',
    // Add authorization header if needed
//  }
//});

//export default api;